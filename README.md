# printer-simulation
A Python script to simulate activity such as printing documents, images, etc

Para crear un ejecutable con pyinstaller:
```
pyinstaller \
  --clean \
  --onefile \
  --name printer-simulation \
  printer_simulation.py

```